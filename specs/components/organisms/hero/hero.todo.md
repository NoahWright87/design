# Hero — Backlog

## Sooner
- [ ] TBD

## Later
- [ ] Optional eyebrow/kicker slot above the title, for a small label distinct from the title itself.
- [ ] Alignment options (e.g. centered, no media) for a simpler landing-page variant.

## Backlog
- [x] Background treatment props (color, image, gradient) for a full-bleed hero section. (`background`/`bottomBorder` cover tinted color + gradient bar; a full-bleed/image-background variant remains open.)
- [ ] Full-bleed background variant that spans the viewport width regardless of an outer container's padding, and a background-image option.
