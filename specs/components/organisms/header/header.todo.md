# Header — Backlog

## Sooner
- [x] Background support: Add a `background` prop (or slot) to easily set a custom background color, gradient, or image.
- [x] Bottom border / shadow: Add a configurable bottom border with shadow support. Include sensible defaults (e.g., subtle drop shadow) with props to override.
- [x] Icon + label layout: When a slot contains both an icon and a label, render the icon above the label with the label styled as a small muted caption.
- [x] `isSticky` prop + story: Add `isSticky` boolean prop for sticky positioning. Add a Storybook example showing sticky behavior with enough scrollable page content to see it in action. (Promoted from Backlog.)

## Later
- [ ] TBD

## Backlog
- Dropdown menus: Built-in navigation menu support (currently composition-based).
- Theme toggle: Built-in light/dark mode switcher (currently application responsibility).
- Responsive drawer: Mobile-optimized menu drawer (currently composition).
- Search bar: Optional search input in center slot.
- Notification badge: Built-in badge for center slot indicators.
