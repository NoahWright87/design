# Pill — Todo

## Sooner
- [x] Neutral default color: The current default (purple) feels too prominent. Change the default to a more subtle/plain color.
- [x] Hover tooltip label: Option to hide the label by default and reveal it as a speech-bubble tooltip on hover.
- [x] Link variant: Add an `href` prop that makes the Pill act as a link; show an outlined border on hover.
- [x] Dismiss animation: Animate the removal of a dismissable Pill. Should have a whimsical variant enabled by default.

## Later
- [x] Dismissable variant (with close button) — implemented via `onDismiss` prop on Pill.
- [x] Icon support (icon before text).

## Backlog
- [x] Custom color support beyond the semantic palette.
