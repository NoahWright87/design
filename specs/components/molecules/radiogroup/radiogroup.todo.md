# RadioGroup — Todo

## Sooner
- [x] Enhanced selection animation: Add more movement when selecting/deselecting options — satisfying and whimsical.
- [x] Error waggle: Side-to-side waggle animation when an error message appears. Example story should be reactive to showcase this.
- [x] Required field validation: Show error state when a required field is left blank after interaction.
- [ ] Even more whimsical animations: Current animations are a start but RadioGroup should have more personality. (Specifics TBD with user.)
- [x] BUG — Inline validation story untriggerable: The inline validation example has no submit button and radios can't be deselected, so there's no way to trigger the error state. Fix the story so the error is demonstrable.

## Later
- [x] Random disabled cursor: When `disabled`, optionally show a randomly selected "no" emoji cursor (🙅 ❌ ⛔ 🚫) via a `randomDisabledCursor` prop.

## Backlog
- [ ] TBD
